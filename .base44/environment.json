{
  "previewPort": 3000,
  "healthPath": "/",
  "startCommand": "docker compose -f docker-compose.base44.yml up -d",
  "secrets": [
    {
      "name": "VITE_SUPABASE_URL",
      "description": "Supabase project URL (e.g. https://xxxxxxxx.supabase.co) — found in Supabase Dashboard → Project Settings → API",
      "requiredAtBoot": false
    },
    {
      "name": "VITE_SUPABASE_ANON_KEY",
      "description": "Supabase anon/public API key — found in Supabase Dashboard → Project Settings → API → Project API keys",
      "requiredAtBoot": false
    }
  ]
}
